# Discord Clone

Веб-приложение для общения в реальном времени.

## Возможности

- 💬 Реальное общение через WebSocket
- 👥 Список пользователей онлайн
- 📨 Личные сообщения
- 👫 Друзья (отдельная вкладка)
- 🎤 Голосовые каналы
- 📎 Отправка файлов
- 🖼️ Кастомные аватарки
- 👻 Режим "Невидимый" (показывается как "не в сети")
- 🎨 Только SVG иконки

## Деплой

### Фронтенд на Netlify

1. Создай репозиторий на GitHub
2. Подключи к Netlify
3. Netlify автоматически задеплоит папку `public`

### WebSocket сервер

Для реального общения нужен WebSocket сервер. Варианты:

**Heroku:**
```bash
heroku create your-app-name
git push heroku main
```

**Railway:**
1. Создай проект на railway.app
2. Подключи репозиторий
3. Railway автоматически запустит `server.js`

**Render:**
1. Создай Web Service на render.com
2. Build Command: `npm install`
3. Start Command: `node server.js`

После деплоя сервера, обнови `WS_URL` в `public/app.js`:
```javascript
const WS_URL = 'wss://your-server.herokuapp.com';
```

## Локальная разработка

```bash
# Установи зависимости
npm install

# Запусти WebSocket сервер
npm run server

# Открой public/index.html в браузере
# или используй live-server
npx live-server public
```

## Структура

```
├── public/           # Фронтенд (деплоится на Netlify)
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── server.js         # WebSocket сервер
├── netlify.toml      # Конфиг Netlify
└── package.json
```